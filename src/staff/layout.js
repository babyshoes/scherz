export const width = 1350;
export const height = 400;

export const marginX = 0;
export const marginTop = 0;
export const marginBottom = height / 25;

export const headerHeight = height / 15;
export const headerFontSize = height * (3/5);
export const footerHeight = height / 20;

export const arrowWidth = width / 80;
export const arrowHeight = headerHeight / 2.5;

export const staffWidth = (width - (marginX * 2));
export const staffHeight = (height - (headerHeight + footerHeight) - (marginTop + marginBottom));

export const chordsMarginX = width / 10;
export const chordsStart = marginX + chordsMarginX;
export const chordsEnd = width - marginX - chordsMarginX;

export const noteSpacing = staffHeight / 28;
export const lineSpacing = staffHeight / 14;

export const circleRadius = height / 40;
export const ledgerLineWidth = circleRadius * 3
export const offsetWidth = width / 50;

export const fontSize = height / 24;
